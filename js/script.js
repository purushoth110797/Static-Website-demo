function scrollToMenu() {
    const section = document.getElementById("explore-menu-section-id");
    if (section) {
        section.scrollIntoView({ behavior: "smooth" });
    }
}
function scrollToPayment() {
    const section = document.getElementById("delivery-and-payment-section-id");
    if (section) {
        section.scrollIntoView({ behavior: "smooth" });
    }
}